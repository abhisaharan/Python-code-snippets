"""
import os
import sys
import time
import address.city.state

from address.city import state
from address import city as city

from lib.mist import LibBase

class A(LibBase):
    def __init__(self, *args, **kw):
        super(A, self).__init__(*args, **kw)

class B(object):
    def __init__(self, *args, **kw)
        super(B, self).__init__(*args, **kw)

    def method1(self):
        s = DeviceContext(self.dut, "what", "when")
        self.dut.Identify()
        return self.dut.IdentifyDrive()

    def method2(self):
        c = SomeContext(self.testInstance.dut, "what")
        return self.testInstance.dut.DriveIdentify()

    def method3(self):
        t = time.time()
        t.clock()
        t.sleep(4)
"""
